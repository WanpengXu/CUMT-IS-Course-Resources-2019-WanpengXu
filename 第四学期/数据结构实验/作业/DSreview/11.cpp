#include <iostream>
#include <cmath>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false),cin.tie(0),cout.tie(0);
    int N;
    cin>>N;
    cout<<int(pow(2,N));//double pow(double x,double y);
    return 0;
}